# Logs de runtime (gerados ao executar o configurador)
logs/
*.log

# Artefatos de build: ignora dist/ exceto os arquivos PUBLICADOS (servidos via irm).
dist/*
!dist/bootstrap.ps1
!dist/payload.ps1
!dist/payload.ps1.sha256

# Lixo do Windows
Thumbs.db
desktop.ini
